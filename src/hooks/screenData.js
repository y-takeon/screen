import { ref, onMounted, onUnmounted } from 'vue'


const mockData = {
}


export default function () {
  const userData = ref({})

  onMounted(() => {
  })

  onUnmounted(() => {
  })

  return {
  }
}
